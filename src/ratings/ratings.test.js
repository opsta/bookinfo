test('adds 1 + 1 to equal 2', () => {
  expect(1 + 1).toBe(2);
});

test('adds 1 + 2 to equal 3', () => {
  expect(1 + 2).toBe(3);
});

test('adds 2 + 1 to equal 3', () => {
  expect(2 + 1).toBe(3);
});

test('adds 2 + 2 to equal 4', () => {
  expect(2 + 2).toBe(4);
});
